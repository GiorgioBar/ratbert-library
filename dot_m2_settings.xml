<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                      http://maven.apache.org/xsd/settings-1.0.0.xsd">

  <activeProfiles>
    <activeProfile>github</activeProfile>
  </activeProfiles>

  <profiles>
    <profile>
      <id>github</id>
      <repositories>
        <!-- 
        In the repositories tag, configure a repository by mapping the id 
        of the repository to the id you added in the server tag containing 
        your credentials. Because upper case letters aren't supported, you 
        must use lowercase letters for the repository owner even if the 
        GitHub user or organization name contains uppercase letters.
        -->
        <repository>
          <id>github</id>
          <name>GitHub GiorgioBar Apache Maven Packages</name>
          <url>https://maven.pkg.github.com/giorgiobar/ratbert-library</url>
        </repository>
      </repositories>
    </profile>
  </profiles>

  <servers>
    <server>
      <id>github</id>
      <username>GiorgioBar</username>
      <password>TOKEN</password>
    </server>
  </servers>
</settings>
